# Backend API service (FastAPI). Install into the repo .venv:
#   .venv/bin/pip install -r backend/requirements.txt
# All of these are already present in the committed .venv.
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
SQLAlchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
